{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Backend for phonebook",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook/ && rm -rf dist/ && npm run build && cp -r dist ../../part3/phonebook-backend"
  },
  "author": "Lokesh",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.17.1",
    "morgan": "^1.10.1"
  }
}
